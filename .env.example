# Copia este archivo a `.env` y rellena los valores reales.
# NO subas `.env` al repositorio (ya está en .gitignore).

# Conexión a Supabase Postgres — usa el Transaction pooler (puerto 6543)
# Reemplaza TU_PASSWORD y, si es necesario, la región (us-east-1, sa-east-1, etc.)
DATABASE_URL=postgresql://postgres.kicalhpqppkknqtjhtml:TU_PASSWORD@aws-0-us-east-1.pooler.supabase.com:6543/postgres

# Clave de sesión Flask (en producción usa una cadena aleatoria larga)
SECRET_KEY=unimag-cec-2026-cambia-este-valor

# Opcional: forzar puerto local
# PORT=5050
